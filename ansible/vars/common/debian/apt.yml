---
apt_keys:
  - https://deb.nodesource.com/gpgkey/nodesource.gpg.key
  - https://dl.yarnpkg.com/debian/pubkey.gpg
  - https://download.docker.com/linux/ubuntu/gpg

apt_repos:
  - ppa:git-core/ppa
  - ppa:fish-shell/release-3
  - deb https://deb.nodesource.com/node_14.x focal main
  - deb https://dl.yarnpkg.com/debian/ stable main
  - deb [arch=amd64] https://download.docker.com/linux/ubuntu focal stable
  - deb [arch=amd64] http://archive.ubuntu.com/ubuntu groovy main
  - deb [arch=amd64] http://archive.ubuntu.com/ubuntu groovy universe

apt_items:
  # Most common terminal stuffs
  - fish
  - ripgrep
  - git
  - git-lfs
  # Addinational terminal stuffs
  - htop
  - tmux
  - nano
  - neovim
  # Optional, but common utilities
  - rsync
  - curl
  - wget
  - wrk
  #  - openssh-server
  # Addinational tools
  - nodejs
  - yarn
  # Build tools
  - cmake
  # Docker
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-compose

apt_force_items:
  - bat
  - exa
  - ripgrep
