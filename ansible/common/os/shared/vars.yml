---
initial_configs:
  # Files
  - .nanorc
  # Folders
  - .nano
  - .config

shared_packages:
  # Required
  - ansible
  # Most common terminal stuffs
  - fish
  - git
  - git-lfs
  # Addinational terminal stuffs
  - nano
  # Optional, but common utilities
  - rsync
  - curl
  - wget
  # Addinational tools
  - yarn
  # Build tools
  - cmake
