---
ensure_folders:
  - "~/.config/nvim"
  - "~/.vim/autoload"

# Initial files to link
initial_configs:
  # Files
  - .vimrc
  # Folders
  - .vim
  - .config/nvim

shared_packages:
  # Addinational terminal stuffs
  - htop
  - tmux
  - neovim
  # Build tools
  - unzip

npm_modules:
  - pm2
